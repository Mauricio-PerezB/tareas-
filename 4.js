// Crear un array con 5 frutas
let frutas = ["Manzana", "Banana", "Naranja", "Fresa", "Kiwi"];

// Recorrer el array con un bucle for
for (let i = 0; i < frutas.length; i++) {
    console.log(frutas[i]);
}