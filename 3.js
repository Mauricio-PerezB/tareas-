// Función que recibe un nombre y devuelve un saludo
function saludar(nombre) {
    return `Hola, ${nombre}!`;
}

// Ejemplo de uso
let saludo = saludar("Mauricio"); // Reemplaza  con el nombre que quieras 
console.log(saludo); // Imprime 