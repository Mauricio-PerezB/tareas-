// Crear un array de 3 números
let numeros = [1, 2, 3];

// Usar map para multiplicar cada número por 2
let numerosMultiplicados = numeros.map(num => num * 2);

// Mostrar el nuevo array en la consola
console.log(numerosMultiplicados); // [2, 4, 6]